# Repository instructions

- This repository must use Laravel’s native migration and seeding system. JSON schema files may be used as source input for code generation, but must not directly mutate the database at runtime.
